<%# メッセージの非同期通信 %>
$('.message').append("<%= j(render("message", chat: @chat)) %>");
$('#chat_message').val("")

<%# 最新のメッセージまでスクロール %>
$('.message').animate({scrollTop: $('.message')[0].scrollHeight}, 'fast');